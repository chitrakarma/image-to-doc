<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>ITED</title>
        <link rel="stylesheet" href="{{ url_for('static', filename='index.css') }}" />
        <link rel="stylesheet" href="{{ url_for('static', filename='bootstrap.css') }}" />
    </head>
    <body>
        <h1 class="jumbotron text-center main-heading">Images to Editable Documents Converter</h1>
        <div class="container">
            <div class="row">
                <div class="col-lg-12 mt-3">
                    <form method="post" action="{{ url_for('upload_file') }}" class="form" enctype="multipart/form-data">
                        <div class="input-group mb-3">
                            <input type="file" class="form-control" />
                            <div class="input-group-append">
                                <button class="btn btn-outline-secondary upload-btn" type="submit">Upload Image</button>
                            </div>
                            <div class="input-group-append">
                                <button class="btn btn-outline-secondary download-btn" type="submit" disabled>Download Document</button>
                            </div>
                        </div>
                    </form>
                </div>
            </button>
        </div>
    </body>
</html>
